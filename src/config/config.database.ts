import { PgProvider } from '../providers/pg'

/**
 * --------------------------------------------------------------------------
 * Postgres Connection
 * --------------------------------------------------------------------------
 * For configuration issue just see the file imported
 */
export const pgClient = new PgProvider()
